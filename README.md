🏋🏽‍♂️ tare
tare is a lightweight terminal agent that cuts the bloat from your prompts before they hit expensive APIs. It uses a cheap LLM to compress and structure your input, then forwards the dense version to the premium model — saving tokens, cost, and latency without losing intent.
